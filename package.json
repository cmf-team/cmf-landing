{
  "name": "cmf-landing",
  "version": "1.0.0",
  "private": true,
  "description": "CMF — Center of Mathematical Finance landing site",
  "scripts": {
    "dev": "echo 'dev  -> http://localhost:8000  (live sources, edit + refresh)' && python3 -m http.server 8000",
    "preview": "./build.sh && echo '' && echo 'preview -> http://localhost:8001  (production bundle)' && python3 -m http.server 8001 --directory dist",
    "build": "./build.sh",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
